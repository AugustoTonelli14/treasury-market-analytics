name: 'treasury_dbt'
version: '1.0.0'
config-version: 2

profile: 'treasury_dbt'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  treasury_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
